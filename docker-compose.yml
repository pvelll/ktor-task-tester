#docker-compose up --build - for launch
version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8082:8082"
    networks:
      - app-network
    volumes:
      - shared-data:/app/shared

  cpp-compiler:
    build:
      context: ./docker/cpp
      dockerfile: DockerfileCpp
    ports:
      - "8083:8083"
    networks:
      - app-network
    volumes:
      - shared-data:/app/shared

  cpp-tester:
    build:
      context: ./docker/cpp-tester
      dockerfile: DockerfileTesterCpp
    ports:
      - "8084:8084"
    networks:
      - app-network
    volumes:
      - shared-data:/app/shared

networks:
  app-network:
    driver: bridge

volumes:
  shared-data:

  # Добавьте другие компиляторы аналогично
  # python-compiler:
  #   image: python:latest
  #   volumes:
  #     - /app