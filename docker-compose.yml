services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8083:8083"

  cpp-compiler:
    build:
      context: ./docker
      dockerfile: DockerfileCpp
    volumes:
      - /app

  # Добавьте другие компиляторы аналогично
  # python-compiler:
  #   image: python:latest
  #   volumes:
  #     - /app